import{NextRequest,NextResponse}from"next/server";import{z}from"zod";import{CAPABILITIES,requireCapability}from"@/lib/permissions";import{getBillingSettings,saveBillingSettings}from"@/lib/billing-settings";import{audit}from"@/lib/audit";import{requestContext}from"@/lib/api";export async function GET(){await requireCapability(CAPABILITIES.BILLING_APPROVE);return NextResponse.json(await getBillingSettings());}const schema=z.object({invoicePrefix:z.string().trim().min(2).max(20).regex(/^[A-Z0-9-]+$/),organisationLegalName:z.string().trim().min(2).max(191),organisationAddress:z.string().max(2000),companyNumber:z.string().max(50),vatNumber:z.string().max(50),bankDetails:z.string().max(2000),remittanceInstructions:z.string().max(2000),defaultPaymentTerms:z.string().max(1000),invoiceLogoUrl:z.string().max(250000)});export async function PUT(req:NextRequest){const user=await requireCapability(CAPABILITIES.BILLING_APPROVE),p=schema.safeParse(await req.json().catch(()=>null));if(!p.success)return NextResponse.json({error:"Check the billing settings."},{status:422});const before=await getBillingSettings();await saveBillingSettings(p.data,user.id);await audit("BILLING_SETTINGS_CHANGED",{actorType:"USER",actorId:user.id,entityType:"AppSetting",beforeValue:before,afterValue:p.data,...requestContext(req)});return NextResponse.json(p.data);}