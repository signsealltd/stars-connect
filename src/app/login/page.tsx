"use client";
import Image from "next/image";
import { useState } from "react";
import Link from "next/link";
import { ArrowLeft,LockKeyhole } from "lucide-react";
import { Header } from "@/components/header";
export default function Login(){const[identifier,setIdentifier]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({identifier,password})});if(r.ok)location.href="/dashboard";else setError((await r.json()).error||"Unable to sign in");setBusy(false)}return <main className="shell"><Header/><div className="content"><Link href="/" className="muted" style={{display:"inline-flex",gap:6,textDecoration:"none"}}><ArrowLeft/>Kiosk home</Link><form autoComplete="off" onSubmit={submit} className="card" style={{maxWidth:490,margin:"38px auto",padding:30}}><div className="logo-panel"><Image src="/branding/stars-logo.svg" alt="STARS" width={170} height={125}/></div><h1 style={{fontSize:31,marginBottom:8}}><LockKeyhole size={27}/> Management login</h1><p className="muted">Secure access to STARS Connect management and reporting.</p><label className="form-label" style={{marginTop:20}}>Username<input className="field" type="text" autoCapitalize="none" spellCheck={false} autoComplete="username" value={identifier} onChange={e=>setIdentifier(e.target.value)} required/></label><label className="form-label" style={{marginTop:16}}>Password<input className="field" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/></label>{error&&<p role="alert" className="alert alert-error">{error}</p>}<button className="btn primary" style={{width:"100%",marginTop:22}} disabled={busy}>{busy?"Signing in…":"Sign in"}</button></form></div></main>}
