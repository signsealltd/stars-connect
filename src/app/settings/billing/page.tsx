import{Header}from"@/components/header";import{BillingSettings}from"@/components/billing-settings";import{CAPABILITIES,requireCapability}from"@/lib/permissions";export default async function Page(){await requireCapability(CAPABILITIES.BILLING_APPROVE);return <main className="shell"><Header manager/><div className="content"><h1>Billing settings</h1><p className="muted">Invoice numbering, legal identity, bank and remittance details.</p><BillingSettings/></div></main>}