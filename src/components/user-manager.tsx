"use client";import{appConfirm}from"@/lib/app-dialog";
import{useCallback,useEffect,useState}from"react";import type{Role}from"@prisma/client";import{Pencil,Plus,Trash2}from"lucide-react";
type UserRow={id:string;name:string;email:string;role:Role;active:boolean;createdAt:string};
const empty={name:"",email:"",role:"MANAGER"as Role,password:"",active:true};
export function UserManager(){const[users,setUsers]=useState<UserRow[]>([]),[editing,setEditing]=useState<UserRow|"new"|null>(null),[form,setForm]=useState(empty),[error,setError]=useState(""),[notice,setNotice]=useState(""),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false);const load=useCallback(async()=>{setLoading(true);const r=await fetch("/api/users",{cache:"no-store"});if(!r.ok)throw new Error("Accounts could not be loaded.");setUsers(await r.json());setLoading(false)},[]);useEffect(()=>{load().catch(e=>setError(e.message))},[load]);function open(user?:UserRow){setError("");setNotice("");setEditing(user||"new");setForm(user?{name:user.name,email:user.email,role:user.role,password:"",active:user.active}:empty)}async function save(event:React.FormEvent){event.preventDefault();setBusy(true);setError("");const isNew=editing==="new",body={...form,password:form.password||undefined,...(isNew?{}:{active:form.active})};try{const r=await fetch(isNew?"/api/users":`/api/users/${(editing as UserRow).id}`,{method:isNew?"POST":"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),result=await r.json();if(!r.ok)throw new Error(result.error||"Account could not be saved.");setEditing(null);setNotice(isNew?"Account created.":"Account updated.");await load()}catch(e){setError(e instanceof Error?e.message:"Account could not be saved.")}finally{setBusy(false)}}async function remove(user:UserRow){if(!await appConfirm(`Permanently delete ${user.name}'s account? They will lose access immediately. This cannot be undone.`))return;setBusy(true);setError("");setNotice("");try{const r=await fetch(`/api/users/${user.id}`,{method:"DELETE"}),result=await r.json();if(!r.ok)throw new Error(result.error||"Account could not be deleted.");setNotice("Account deleted.");await load()}catch(e){setError(e instanceof Error?e.message:"Account could not be deleted.")}finally{setBusy(false)}}return <><div className="page-head"><div><h1 className="page-title">Users & Permissions</h1><p className="muted">Create manager accounts, assign roles, reset passwords, disable access or permanently remove accounts.</p></div><button className="btn primary" onClick={()=>open()}><Plus size={18}/>Add user</button></div>{error&&!editing&&<div className="alert alert-error">{error}</div>}{notice&&<div className="alert alert-success">{notice}</div>}<section className="card table-wrap">{loading?<div className="empty">Loading accounts…</div>:<table className="table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Created</th><th>Actions</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td>{user.name}</td><td>{user.email}</td><td>{user.role.replaceAll("_"," ")}</td><td><span className={`badge ${user.active?"badge-success":"badge-danger"}`}>{user.active?"Active":"Disabled"}</span></td><td>{new Date(user.createdAt).toLocaleDateString("en-GB")}</td><td><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><button className="btn ghost" onClick={()=>open(user)} disabled={busy}><Pencil size={16}/>Edit</button><button className="btn ghost" onClick={()=>remove(user)} disabled={busy} style={{color:"var(--danger, #a52a2a)"}}><Trash2 size={16}/>Delete</button></div></td></tr>)}</tbody></table>}</section>{editing&&<div className="modal-backdrop"><form autoComplete="off" className="modal" onSubmit={save}><h2>{editing==="new"?"Add manager account":"Edit account"}</h2>{error&&<div className="alert alert-error">{error}</div>}<div className="form-grid"><label className="form-label">Name<input autoComplete="off" className="field" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="form-label">Email<input autoComplete="off" className="field" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="form-label">Role<select className="field" value={form.role} onChange={e=>setForm({...form,role:e.target.value as Role})}><option value="RECEPTION">Reception</option><option value="MANAGER">Manager</option><option value="DIRECTOR">Director</option><option value="ADMINISTRATOR">Administrator</option></select></label><label className="form-label">Password<input autoComplete="off" className="field" type="password" required={editing==="new"} minLength={12} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder={editing==="new"?"At least 12 characters":"Leave blank to keep current"}/></label>{editing!=="new"&&<label className="full"><input autoComplete="off" type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/> Account active</label>}</div><p className="muted">Passwords require 12 characters with uppercase, lowercase and a number. Password or role changes sign that user out.</p><div className="modal-actions"><button type="button" className="btn secondary" onClick={()=>setEditing(null)}>Cancel</button><button className="btn primary" disabled={busy}>{busy?"Saving…":"Save account"}</button></div></form></div>}</>}
