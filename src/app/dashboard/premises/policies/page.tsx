import "@/app/safety.css";import {Header} from "@/components/header";import {ControlledLibrary} from "@/components/controlled-library";import {requirePageCapability,CAPABILITIES as C} from "@/lib/permissions";export default async function Page(){await requirePageCapability(C.POLICY_VIEW);return <main className="shell"><Header manager/><div className="content"><a href="/dashboard/premises">← Safety &amp; Compliance</a><ControlledLibrary type="POLICY"/></div></main>}
