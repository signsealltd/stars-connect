import{Header}from"@/components/header";import{prisma}from"@/lib/prisma";import{requireRole}from"@/lib/security";export const dynamic="force-dynamic";
export default async function UsersPage(){await requireRole("ADMINISTRATOR");const users=await prisma.user.findMany({select:{id:true,name:true,email:true,role:true,active:true,createdAt:true},orderBy:[{active:"desc"},{name:"asc"}]});return <main className="shell"><Header manager/><div className="content"><h1 className="page-title">Users & Permissions</h1><p className="muted">Manager authentication is separate from kiosk device provisioning. Account changes remain administrator-controlled.</p><section className="card table-wrap"><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Created</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td>{user.name}</td><td>{user.email}</td><td>{user.role.replaceAll("_"," ")}</td><td><span className={`badge ${user.active?"badge-success":"badge-danger"}`}>{user.active?"Active":"Disabled"}</span></td><td>{user.createdAt.toLocaleDateString("en-GB")}</td></tr>)}</tbody></table></section><div className="alert alert-warning">Role and password editing is not yet exposed in V1. Use the controlled database administration procedure for account changes.</div></div></main>}