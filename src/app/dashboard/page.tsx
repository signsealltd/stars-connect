"use client";
import { useEffect,useState } from "react";
import Link from "next/link";
import { AlertTriangle,ClipboardCheck,FileDown,RefreshCw,Siren,Users } from "lucide-react";
import { Header } from "@/components/header";

type Data={role:string;staffIn:number;activeVisitors:number;present:number;absent:number;late:number;expected:number;notMarked:number;missingClockOut:number;review:number;conflicts:number;corrections:number;emergency?:{missing:number};email?:{status:string;sentAt?:string};recentEvents:Array<{id:string;name:string;type:string;time:string}>;recentAttendance:Array<{id:string;name:string;status:string;time:string}>;devices:Array<{id:string;name:string;lastSyncAt?:string;status:string;pendingEventCount:number;stale:boolean}>};
export default function Dashboard(){const[d,setD]=useState<Data>(),[error,setError]=useState("");async function load(){const r=await fetch("/api/dashboard");if(r.status===403){location.href="/login";return}if(!r.ok){setError("Dashboard data could not be loaded.");return}setD(await r.json())}useEffect(()=>{load();const id=setInterval(load,30000);return()=>clearInterval(id)},[]);
return <main className="shell"><Header manager/><div className="content"><div className="page-head"><div><h1 className="page-title">Today at STARS</h1><p className="muted">Live attendance, review items and device health.</p></div><button className="btn secondary no-print" onClick={load}><RefreshCw size={17}/> Refresh</button></div>{error&&<div className="alert alert-error">{error}</div>}{!d?<div className="card empty">Loading dashboard…</div>:<><div className="grid stat-grid"><Link className="card stat" href="/live"><span>Staff currently in</span><b>{d.staffIn}</b></Link><Link className="card stat" href="/live"><span>Students present</span><b>{d.present}</b></Link><Link className="card stat" href="/dashboard/visitors"><span>Visitors on site</span><b>{d.activeVisitors}</b></Link><Link className="card stat" href="/register"><span>Expected today</span><b>{d.expected}</b></Link><Link className="card stat" href="/register"><span>Not yet marked</span><b>{d.notMarked}</b></Link><Link className="card stat" href="/timesheets"><span>Missing clock-outs</span><b style={{color:d.missingClockOut?"var(--warning)":"var(--success)"}}>{d.missingClockOut}</b></Link><Link className="card stat" href="/reports"><span>Absent / late</span><b>{d.absent} / {d.late}</b></Link><Link className="card stat" href="/dashboard/devices"><span>Stale devices</span><b style={{color:d.devices.some(x=>x.stale)?"var(--danger)":"var(--success)"}}>{d.devices.filter(x=>x.stale).length}</b></Link><div className="card stat"><span>Requires review</span><b>{d.review}</b></div></div>{d.emergency&&<div className="alert alert-error" style={{marginTop:18}}><Siren size={18}/> An emergency roll call is active. {d.emergency.missing} people remain unaccounted for. <Link href="/emergency">Open roll call</Link></div>}<div className="grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(300px,1fr))",marginTop:20}}><section className="card" style={{padding:22}}><h2>Recent clock events</h2>{d.recentEvents.length?d.recentEvents.map(e=><div key={e.id} style={{display:"flex",padding:"12px 0",borderTop:"1px solid var(--border)"}}><b>{e.name}</b><span className="muted" style={{marginLeft:"auto"}}>{e.type==="CLOCK_IN"?"In":"Out"} · {new Date(e.time).toLocaleTimeString("en-GB",{hour:"2-digit",minute:"2-digit"})}</span></div>):<p className="muted">No clock events today.</p>}</section><section className="card" style={{padding:22}}><h2>Recent student changes</h2>{d.recentAttendance.length?d.recentAttendance.map(a=><div key={a.id} style={{display:"flex",padding:"12px 0",borderTop:"1px solid var(--border)"}}><b>{a.name}</b><span className="badge badge-neutral" style={{marginLeft:"auto"}}>{a.status.replace("_"," ")}</span></div>):<p className="muted">No student changes today.</p>}</section><section className="card" style={{padding:22}}><h2>Device health</h2>{d.devices.map(device=><div key={device.id} style={{padding:"12px 0",borderTop:"1px solid var(--border)"}}><div style={{display:"flex"}}><b>{device.name}</b><span className={`badge ${device.stale?"badge-danger":"badge-success"}`} style={{marginLeft:"auto"}}>{device.stale?"Stale":"Healthy"}</span></div><small className="muted">{device.pendingEventCount} pending · {device.lastSyncAt?`synced ${new Date(device.lastSyncAt).toLocaleString("en-GB")}`:"never synced"}</small></div>)}</section><section className="card" style={{padding:22}}><h2>Quick actions</h2><div className="grid"><Link className="btn secondary" href="/register"><ClipboardCheck/>Open register</Link><Link className="btn secondary" href="/live"><Users/>Live register</Link><Link className="btn danger" href="/emergency"><Siren/>Emergency roll call</Link><Link className="btn secondary" href="/reports"><FileDown/>Reports</Link>{d.conflicts>0&&<div className="alert alert-warning"><AlertTriangle size={17}/>{d.conflicts} unresolved sync conflicts</div>}</div></section></div></>}</div></main>}
