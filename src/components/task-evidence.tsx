"use client";import {useState} from "react";
export function TaskEvidence({id,onSaved}:{id:string;onSaved:()=>void}){const[url,setUrl]=useState(''),[note,setNote]=useState(''),[error,setError]=useState('');return <details><summary>Add evidence link</summary><form onSubmit={async e=>{e.preventDefault();const r=await fetch('/api/tasks-overview/evidence',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id,url,note})});if(!r.ok)setError((await r.json()).error);else onSaved()}}><label>Evidence URL<input className="field" required value={url} onChange={e=>setUrl(e.target.value)}/></label><label>Evidence note<input className="field" required minLength={3} value={note} onChange={e=>setNote(e.target.value)}/></label>{error&&<p role="alert">{error}</p>}<button className="btn secondary">Save evidence</button></form></details>}
