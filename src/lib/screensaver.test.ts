import{describe,expect,it}from"vitest";import{screensaverAllowedOnRoute,screensaverDefaults,screensaverSchema,visualPeriod}from"./screensaver";describe("kiosk screensaver",()=>{it("uses safe defaults",()=>{expect(screensaverSchema.safeParse(screensaverDefaults).success).toBe(true);expect(screensaverDefaults.idleTimeoutSeconds).toBe(30);expect(screensaverDefaults.backgroundAnimationStyle).toBe("constellation");expect(screensaverDefaults.wakeTransitionSeconds).toBeGreaterThan(0)});it("selects configured day evening and night brightness",()=>{expect(visualPeriod(new Date(2026,6,26,8),screensaverDefaults)).toEqual({mode:"day",brightness:45});expect(visualPeriod(new Date(2026,6,26,20),screensaverDefaults)).toEqual({mode:"evening",brightness:25});expect(visualPeriod(new Date(2026,6,26,23),screensaverDefaults)).toEqual({mode:"night",brightness:10});expect(visualPeriod(new Date(2026,6,26,3),screensaverDefaults).mode).toBe("night")});it("never covers interactive or emergency workflows",()=>{expect(screensaverAllowedOnRoute("/")).toBe(true);for(const route of["/clock","/register","/visitors","/setup","/emergency"])expect(screensaverAllowedOnRoute(route)).toBe(false)})});