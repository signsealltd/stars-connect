import{describe,expect,it}from"vitest";import{dailyJobKey,jobDisposition}from"./scheduled-reports";describe("scheduled daily report locking",()=>{const now=new Date("2026-07-26T00:05:00Z");it("uses a stable organisation/date/version idempotency key",()=>expect(dailyJobKey("2026-07-25")).toBe("daily-report:stars:2026-07-25:v1"));it("skips completed duplicate invocation",()=>expect(jobDisposition({status:"SUCCESS",lockExpiresAt:now},now)).toBe("ALREADY_PROCESSED"));it("rejects a concurrent live lock",()=>expect(jobDisposition({status:"RUNNING",lockExpiresAt:new Date(now.getTime()+60_000)},now)).toBe("ALREADY_RUNNING"));it("allows a failed job retry",()=>expect(jobDisposition({status:"FAILED",lockExpiresAt:now},now)).toBe("ACQUIRE"));it("recovers an expired execution lock",()=>expect(jobDisposition({status:"RUNNING",lockExpiresAt:new Date(now.getTime()-1)},now)).toBe("ACQUIRE"));});