"use client";import{useEffect,useState}from"react";type S={clockInAllowanceMinutes:number;clockOutAllowanceMinutes:number;roundingIntervalMinutes:15};
export function TransportSettings(){const[s,setS]=useState<S>(),[message,setMessage]=useState("");useEffect(()=>{fetch("/api/settings/transport").then(r=>r.json()).then(setS)},[]);async function save(e:React.FormEvent){e.preventDefault();if(!s)return;const r=await fetch("/api/settings/transport",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(s)}),j=await r.json();setMessage(r.ok?"Transport settings saved.":j.error||"Unable to save.")}if(!s)return<div className="empty">Loading transport settings…</div>;return<form className="card form-grid" onSubmit={save}><label>Minutes added before transport clock-in<input className="field" type="number" min="0" max="240" value={s.clockInAllowanceMinutes} onChange={e=>setS({...s,clockInAllowanceMinutes:Number(e.target.value)})}/></label><label>Minutes added after transport clock-out<input className="field" type="number" min="0" max="240" value={s.clockOutAllowanceMinutes} onChange={e=>setS({...s,clockOutAllowanceMinutes:Number(e.target.value)})}/></label><label>Payroll rounding<select className="field" value="15" disabled><option value="15">Nearest 15 minutes</option></select></label><p className="muted span-2">Payroll retains the original minutes and shows transport and rounding changes for review before approval.</p>{message&&<div className={message.includes("saved")?"alert alert-success span-2":"alert alert-error span-2"}>{message}</div>}<button className="btn primary">Save transport setup</button></form>}
