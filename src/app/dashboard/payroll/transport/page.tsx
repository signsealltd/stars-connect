import{Header}from"@/components/header";import{TransportSettings}from"@/components/transport-settings";import{CAPABILITIES,requirePageCapability}from"@/lib/permissions";export default async function Page(){await requirePageCapability(CAPABILITIES.PAYROLL_REVIEW);return<main className="shell"><Header manager/><div className="content"><h1>Transport setup</h1><p className="muted">Configure paid time added when staff tag a clock event as student transport.</p><TransportSettings/></div></main>}
