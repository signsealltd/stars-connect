"use client";import {useEffect,useState} from "react";import {usePathname} from "next/navigation";
export function WorkflowHighlight(){const path=usePathname(),[message,setMessage]=useState('');useEffect(()=>{const key=new URLSearchParams(location.search).get('guide');if(!key||!['create-task','task-owner','new-controlled-document','attention','new-rams'].includes(key))return;let attempts=0;let target:HTMLElement|null=null;const timer=setInterval(()=>{target=document.querySelector(`[data-guide="${key}"]`);if(target){clearInterval(timer);target.classList.add('clive-highlight');target.scrollIntoView({block:'center',behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'});setMessage('Highlighted the relevant control.');}else if(++attempts>15){clearInterval(timer);setMessage('That control is not currently available. Check your access or open the record first.')}},250);const clear=setTimeout(()=>{target?.classList.remove('clive-highlight');setMessage('')},6500);return()=>{clearInterval(timer);clearTimeout(clear);target?.classList.remove('clive-highlight')}},[path]);return message?<div role="status" className="clive-guide-status">{message}</div>:null}
