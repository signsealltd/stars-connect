import {it,expect,vi} from "vitest";
const query=vi.hoisted(()=>vi.fn(async()=>[{id:"assignment",confirmation:"PENDING",occurrence:{startAt:new Date("2026-10-01T08:00:00Z"),endAt:new Date("2026-10-01T09:00:00Z"),status:"PLANNING",location:"Hall",staffVisibleNotes:"Art session",staffConfirmationRequired:false,operation:{title:"Art activity",type:"ACTIVITY"}}}]));
vi.mock("./prisma",()=>({prisma:{staffWorkingPattern:{findMany:async()=>[]},staffScheduleOccurrence:{findMany:async()=>[]},staffScheduleException:{findMany:async()=>[]},operationStaffAssignment:{findMany:query}}}));
import {personalSchedule} from "./staff-area-data";
it("shows planned calendar assignments only through the signed-in staff and organisation query",async()=>{const rows=await personalSchedule("assigned-person","org","2026-10");expect(query.mock.calls[0]).toBeDefined();expect(query).toHaveBeenCalledWith(expect.objectContaining({where:expect.objectContaining({staffId:"assigned-person",organisationId:"org",status:"ASSIGNED",occurrence:expect.objectContaining({status:{not:"DRAFT"}})})}));expect(rows).toHaveLength(1);expect(rows[0]).toMatchObject({title:"Art activity",kind:"EVENT",status:"Planned",location:"Hall",notes:"Art session"})});
