import {NextRequest} from "next/server";import {withCapability} from "@/lib/api";import {CAPABILITIES as C} from "@/lib/permissions";import {staffJson} from "@/lib/staff-area-auth";import {taskOverview} from "@/lib/task-overview";export async function GET(req:NextRequest){return withCapability(req,C.STAFF_TASKS,async user=>staffJson(await taskOverview(user)))}
