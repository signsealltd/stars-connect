"use client";import{useState}from"react";import Link from"next/link";import{ArrowLeft,LockKeyhole}from"lucide-react";import{Header}from"@/components/header";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});if(r.ok)location.href="/dashboard";else setError((await r.json()).error||"Unable to sign in");setBusy(false)}return <main className="shell"><Header/><div className="content"><Link href="/" className="muted" style={{display:"inline-flex",gap:6,textDecoration:"none"}}><ArrowLeft/>Kiosk home</Link><form onSubmit={submit} className="card" style={{maxWidth:470,margin:"48px auto",padding:30}}><LockKeyhole size={38} color="#177e59"/><h1 style={{fontSize:31,marginBottom:8}}>Manager login</h1><p className="muted">Secure access to management and reporting.</p><label style={{display:"block",marginTop:20}}>Email<input className="field" type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label style={{display:"block",marginTop:16}}>Password<input className="field" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/></label>{error&&<p role="alert" style={{color:"#a72f2f"}}>{error}</p>}<button className="btn primary" style={{width:"100%",marginTop:22}} disabled={busy}>{busy?"Signing in…":"Sign in"}</button></form></div></main>}
