import{Header}from"@/components/header";import{prisma}from"@/lib/prisma";import{CAPABILITIES,requireCapability}from"@/lib/permissions";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){await requireCapability(CAPABILITIES.PAYROLL_REVIEW);const{id}=await params,p=await prisma.payrollPeriod.findUniqueOrThrow({where:{id},include:{entries:true,adjustments:true}});return <main className="shell"><Header manager/><div className="content"><h1 className="page-title">Payroll period details</h1><p className="muted">{p.status} · version {p.version}</p><section className="card table-wrap"><table className="table"><thead><tr><th>Employee</th><th>Ordinary</th><th>Overtime</th><th>Holiday</th><th>Sickness</th><th>Training</th><th>Unpaid</th><th>Adjustments</th><th>Total</th><th>Exceptions</th></tr></thead><tbody>{p.entries.map(e=><tr key={e.id}><td>{e.staffName}<br/><small>{e.payrollNumber||"No payroll number"}</small></td>{[e.ordinaryMinutes,e.overtimeMinutes,e.holidayMinutes,e.sicknessMinutes,e.trainingMinutes,e.unpaidMinutes,e.adjustmentMinutes,e.totalPayableMinutes].map((n,i)=><td key={i}>{(n/60).toFixed(2)}h</td>)}<td>{e.exceptionStatus} ({e.exceptionCount})</td></tr>)}</tbody></table></section></div></main>}