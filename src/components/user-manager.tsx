"use client";
import {useCallback,useEffect,useState} from "react";
import {Pencil,Plus,Trash2} from "lucide-react";
import {appConfirm} from "@/lib/app-dialog";
import {capabilityOptions,hasCapability,type Capability} from "@/lib/permission-catalog";
import type {Role} from "@prisma/client";
type UserRow={accessLevelId?:string|null;id:string;name:string;username:string;email:string|null;role:Role;active:boolean;createdAt:string;permissionOverrides?:Record<string,boolean>|null};
const blank=(role:Role="MANAGER")=>({name:"",username:"",email:"",role,password:"",active:true,permissionOverrides:Object.fromEntries(capabilityOptions.map(x=>[x.key,hasCapability(role,x.key)])) as Record<Capability,boolean>});
export function UserManager(){
  const[users,setUsers]=useState<UserRow[]>([]),[editing,setEditing]=useState<UserRow|"new"|null>(null),[form,setForm]=useState(blank()),[resetPassword,setResetPassword]=useState(false),[error,setError]=useState(""),[notice,setNotice]=useState(""),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false);
  const load=useCallback(async()=>{setLoading(true);const r=await fetch("/api/users",{cache:"no-store"});if(!r.ok)throw new Error("Accounts could not be loaded.");setUsers(await r.json());setLoading(false)},[]);
  useEffect(()=>{load().catch(e=>setError(e.message))},[load]);
  function permissions(role:Role,custom?:Record<string,boolean>|null){return Object.fromEntries(capabilityOptions.map(x=>[x.key,custom?.[x.key]??hasCapability(role,x.key)])) as Record<Capability,boolean>}
  function open(user?:UserRow){setError("");setNotice("");setResetPassword(false);setEditing(user||"new");setForm(user?{name:user.name,username:user.username,email:user.email||"",role:user.role,password:"",active:user.active,permissionOverrides:permissions(user.role,user.permissionOverrides)}:blank())}
  function roleChanged(role:Role){setForm({...form,role,permissionOverrides:permissions(role)})}
  async function save(event:React.FormEvent){event.preventDefault();setBusy(true);setError("");const isNew=editing==="new",body={...form,...(!isNew&&(editing as UserRow).accessLevelId?{role:undefined,permissionOverrides:undefined}:{}),password:(isNew||resetPassword)?form.password||undefined:undefined,...(isNew?{}:{active:form.active})};try{const r=await fetch(isNew?"/api/users":`/api/users/${(editing as UserRow).id}`,{method:isNew?"POST":"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),result=await r.json();if(!r.ok)throw new Error(result.error||"Account could not be saved.");setEditing(null);setNotice(isNew?"Account created.":"Account and permissions updated. The user has been signed out so changes take effect.");await load()}catch(e){setError(e instanceof Error?e.message:"Account could not be saved.")}finally{setBusy(false)}}
  async function remove(user:UserRow){if(!await appConfirm(`Permanently delete ${user.name}'s account? They will lose access immediately. This cannot be undone.`))return;setBusy(true);setError("");setNotice("");try{const r=await fetch(`/api/users/${user.id}`,{method:"DELETE"}),result=await r.json();if(!r.ok)throw new Error(result.error||"Account could not be deleted.");setNotice("Account deleted.");await load()}catch(e){setError(e instanceof Error?e.message:"Account could not be deleted.")}finally{setBusy(false)}}
  return <><div className="page-head"><div><h1 className="page-title">Users & Permissions</h1><p className="muted">Role defaults provide a safe starting point. Individual finance, report, document and visitor-contact permissions can be adjusted.</p></div><button className="btn primary" onClick={()=>open()}><Plus size={18}/>Add user</button></div>{error&&!editing&&<div className="alert alert-error">{error}</div>}{notice&&<div className="alert alert-success">{notice}</div>}<section className="card table-wrap">{loading?<div className="empty">Loading accounts…</div>:<table className="table"><thead><tr><th>Name</th><th>Username</th><th>Email</th><th>Role</th><th>Permissions</th><th>Status</th><th>Actions</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td>{user.name}</td><td><b>{user.username}</b></td><td>{user.email||"Not set"}</td><td>{user.role.replaceAll("_"," ")}</td><td>{Object.values(permissions(user.role,user.permissionOverrides)).filter(Boolean).length} of {capabilityOptions.length}</td><td><span className={`badge ${user.active?"badge-success":"badge-danger"}`}>{user.active?"Active":"Disabled"}</span></td><td><div className="table-actions"><button className="btn ghost" onClick={()=>open(user)} disabled={busy}><Pencil size={16}/>Edit</button><button className="btn ghost" onClick={()=>remove(user)} disabled={busy} style={{color:"var(--danger, #a52a2a)"}}><Trash2 size={16}/>Delete</button></div></td></tr>)}</tbody></table>}</section>{editing&&<div className="modal-backdrop"><form autoComplete="off" className="modal" onSubmit={save}><h2>{editing==="new"?"Add manager account":"Edit account and permissions"}</h2>{error&&<div className="alert alert-error">{error}</div>}<div className="form-grid"><label className="form-label">Name<input autoComplete="off" className="field" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="form-label">Username<input autoCapitalize="none" spellCheck={false} autoComplete="off" className="field" required value={form.username} onChange={e=>setForm({...form,username:e.target.value.toLowerCase()})}/></label><label className="form-label">Email (optional)<input autoComplete="off" className="field" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="form-label">Role<select disabled={editing!=="new"&&!!editing.accessLevelId} className="field" value={form.role} onChange={e=>roleChanged(e.target.value as Role)}><option value="TEAM_LEADER">Team Leader</option><option value="CARE_ASSISTANT">Care Assistant</option><option value="RECEPTION">Reception</option><option value="MANAGER">Manager</option><option value="DIRECTOR">Director</option><option value="ADMINISTRATOR">Administrator</option></select></label>{editing==="new"||resetPassword?<label className="form-label">{editing==="new"?"Password":"New password"}<input autoComplete="new-password" className="field" type="password" required minLength={12} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 12 characters"/><small>Use at least 12 characters, including uppercase, lowercase and a number.</small></label>:<div className="form-label"><span>Password</span><button type="button" className="btn secondary" onClick={()=>{setForm({...form,password:""});setResetPassword(true)}}>Reset password</button><small className="muted" style={{display:"block",marginTop:6}}>The current password will remain unchanged.</small></div>}{editing!=="new"&&<label className="full check-row"><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/>Account active</label>}<fieldset disabled={editing!=="new"&&!!editing.accessLevelId} className="full card" style={{padding:16}}><legend><b>Individual permissions</b></legend><p className="muted">Changing the role resets these switches to that role’s recommended defaults. Users, backups and system maintenance have separate permissions. Linked staff accounts use their assigned access level.</p><div className="grid">{capabilityOptions.map(option=><label className="check-row" key={option.key}><input type="checkbox" checked={form.permissionOverrides[option.key]} onChange={e=>setForm({...form,permissionOverrides:{...form.permissionOverrides,[option.key]:e.target.checked}})}/><span><b>{option.label}</b><small style={{display:"block"}}>{option.description}</small></span></label>)}</div></fieldset></div><p className="muted">Permission, password or role changes sign that user out immediately.</p><div className="modal-actions"><button type="button" className="btn secondary" onClick={()=>setEditing(null)}>Cancel</button><button className="btn primary" disabled={busy}>{busy?"Saving…":"Save account"}</button></div></form></div>}</>
}
