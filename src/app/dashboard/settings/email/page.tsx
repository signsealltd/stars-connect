import{Header}from"@/components/header";import{EmailSettingsPanel}from"@/components/email-settings-panel";import{CAPABILITIES,requireCapability}from"@/lib/permissions";export const dynamic="force-dynamic";export default async function Page(){await requireCapability(CAPABILITIES.REPORT_SETTINGS_MANAGE);return <main className="shell"><Header manager/><div className="content"><h1 className="page-title">Email</h1><p className="muted">Safe SMTP diagnostics and test delivery. Credentials and job secrets are never displayed.</p><EmailSettingsPanel/></div></main>}