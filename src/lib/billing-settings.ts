import{Prisma}from"@prisma/client";import{prisma}from"./prisma";export const BILLING_SETTING_DEFAULTS={invoicePrefix:"STARS",organisationLegalName:"STARS Day Service",organisationAddress:"",companyNumber:"",vatNumber:"",bankDetails:"",remittanceInstructions:"",defaultPaymentTerms:"Payment is due by the date shown.",invoiceLogoUrl:""};export async function getBillingSettings(){const rows=await prisma.appSetting.findMany({where:{key:{in:Object.keys(BILLING_SETTING_DEFAULTS)}}});return{...BILLING_SETTING_DEFAULTS,...Object.fromEntries(rows.map(r=>[r.key,r.value]))}as typeof BILLING_SETTING_DEFAULTS;}export async function saveBillingSettings(values:typeof BILLING_SETTING_DEFAULTS,userId:string){await prisma.$transaction(Object.entries(values).map(([key,value])=>prisma.appSetting.upsert({where:{key},update:{value:value as Prisma.InputJsonValue,updatedBy:userId},create:{key,value:value as Prisma.InputJsonValue,updatedBy:userId}})));}