"use client";
import {useEffect,useState} from "react";
export function FleetAlert(){const [count,setCount]=useState(0);useEffect(()=>{let live=true;const load=async()=>{const access=await fetch("/api/auth/me");if(!access.ok)return;const me=await access.json();if(!me.capabilities?.includes("fleet.view"))return;const r=await fetch("/api/fleet/vehicles");if(r.ok&&live){const d=await r.json();setCount(d.rows.filter((v:{status:string})=>v.status==="OUT_OF_SERVICE").length)}};void load();const timer=setInterval(()=>void load(),30000);return()=>{live=false;clearInterval(timer)}},[]);return count?<a className="alert alert-error" style={{display:"block",marginBottom:18}} href="/dashboard/premises/fleet"><b>DO NOT USE: {count} vehicle{count===1?"":"s"} out of service</b> — review Fleet defects</a>:null}
