import "@/app/safety.css";import {Header} from "@/components/header";import {AssetDetail} from "@/components/asset-detail";import {requirePageCapability,CAPABILITIES as C} from "@/lib/permissions";export default async function Page({params}:{params:Promise<{token:string}>}){await requirePageCapability(C.PREMISES_VIEW);const {token}=await params;return <main className="shell"><Header manager/><div className="content"><a href="/dashboard/premises?section=assets">← Asset Management</a><AssetDetail token={token}/></div></main>}
