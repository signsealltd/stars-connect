import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";
describe("temporary twinkling-star screensaver artwork",()=>{it("routes every monthly choice through one shared placeholder",()=>{const dispatcher=readFileSync("src/components/screensavers/scenes/index.tsx","utf8");expect(dispatcher).toContain("TwinklingStarsScene");expect(dispatcher).not.toContain("JanuaryWinterScene")});it("uses deterministic CSS-only twinkling without timers",()=>{const scene=readFileSync("src/components/screensavers/scenes/TwinklingStarsScene.tsx","utf8"),css=readFileSync("src/app/globals.css","utf8");expect(scene).toContain('className="placeholder-star"');expect(scene).not.toContain("setInterval");expect(css).toContain(".placeholder-star")})});
