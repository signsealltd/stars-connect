import{Header}from"@/components/header";import{prisma}from"@/lib/prisma";import{CAPABILITIES,requireCapability}from"@/lib/permissions";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){await requireCapability(CAPABILITIES.BILLING_REVIEW);const{id}=await params,r=await prisma.billingRun.findUniqueOrThrow({where:{id},include:{charges:true,invoices:true}});return <main className="shell"><Header manager/><div className="content"><h1>Billing run details</h1><p>{r.status} · version {r.version}</p><section className="card table-wrap"><table className="table"><thead><tr><th>Service user</th><th>Payer</th><th>Date</th><th>Description</th><th>Quantity</th><th>Net</th><th>VAT</th><th>Total</th><th>Exception</th></tr></thead><tbody>{r.charges.map(c=><tr key={c.id}><td>{c.studentName}</td><td>{c.payerName}</td><td>{c.sourceDate.toLocaleDateString("en-GB")}</td><td>{c.description}</td><td>{String(c.quantity)}</td><td>£{Number(c.netAmount).toFixed(2)}</td><td>£{Number(c.vatAmount).toFixed(2)}</td><td>£{Number(c.grossAmount).toFixed(2)}</td><td>{c.exceptionCode||"—"}</td></tr>)}</tbody></table></section></div></main>}