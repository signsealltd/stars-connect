import{Header}from"@/components/header";import{EmailDeliveryHistory}from"@/components/email-delivery-history";import{CAPABILITIES,requirePageCapability}from"@/lib/permissions";export const dynamic="force-dynamic";export default async function Page(){await requirePageCapability(CAPABILITIES.REPORT_SETTINGS_MANAGE);return <main className="shell"><Header manager/><div className="content"><h1>Email Delivery History</h1><p className="muted">Safe SMTP results only. Message bodies, credentials and transport transcripts are never stored here.</p><EmailDeliveryHistory/></div></main>}