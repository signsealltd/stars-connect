import{describe,expect,it}from"vitest";import{calculateWorkedMinutes,canAccess,isDuplicateEvent,nextClockAction}from"./domain";
const at=(h:number,m=0)=>new Date(2026,6,20,h,m);
describe("clocking",()=>{it("clocks in with no prior event",()=>expect(nextClockAction([])).toBe("CLOCK_IN"));it("clocks out after clock in",()=>expect(nextClockAction([{type:"CLOCK_IN",deviceTimestamp:at(9)}])).toBe("CLOCK_OUT"));it("detects duplicate scans within 20 seconds",()=>expect(isDuplicateEvent([{type:"CLOCK_IN",deviceTimestamp:at(9)}],{type:"CLOCK_IN",deviceTimestamp:new Date(at(9).getTime()+19000)})).toBe(true));it("calculates paired time and flags missing clock-out",()=>{expect(calculateWorkedMinutes([{type:"CLOCK_IN",deviceTimestamp:at(9)},{type:"CLOCK_OUT",deviceTimestamp:at(17,30)}])).toEqual({minutes:510,missingClockOut:false});expect(calculateWorkedMinutes([{type:"CLOCK_IN",deviceTimestamp:at(9)}]).missingClockOut).toBe(true)})});
describe("permissions",()=>{it("prevents reception from payroll reports",()=>expect(canAccess("RECEPTION","reports")).toBe(false));it("allows a manager to see reports",()=>expect(canAccess("MANAGER","reports")).toBe(true))});
