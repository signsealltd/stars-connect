import{Header}from"@/components/header";import{BillingProfiles}from"@/components/billing-profiles";import{requireCapability,CAPABILITIES}from"@/lib/permissions";export const dynamic="force-dynamic";export default async function Page(){await requireCapability(CAPABILITIES.BILLING_APPROVE);return <main className="shell"><Header manager/><div className="content"><h1 className="page-title">Billing profiles</h1><p className="muted">Finance-only payer arrangements and transparent charge rules.</p><BillingProfiles/></div></main>}