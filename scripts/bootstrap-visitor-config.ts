import { PrismaClient } from "@prisma/client";
const prisma=new PrismaClient();
const labels=["Electrical","Fire safety","Plumbing","Maintenance","Delivery","Contractor","Professional visit","Personal visit","Meeting","Other"];
async function main(){for(const[sortOrder,label]of labels.entries())await prisma.visitorReason.upsert({where:{label},update:{sortOrder},create:{label,sortOrder,active:true}});if(!await prisma.visitorRuleSet.findFirst())await prisma.visitorRuleSet.create({data:{version:1,title:"Visitor site rules",rulesText:"Please remain with your host unless instructed otherwise. Follow all fire, emergency and safeguarding instructions. Do not photograph or record people on site. Report hazards immediately and wear any required protective equipment. Sign out before leaving the site.",active:true}});for(const[key,value]of Object.entries({visitorCompanyRequired:false,visitorMobileRequired:false,visitorVehicleRequired:false,visitorDurationRequired:false,visitorRecordRetentionDays:730,visitorSignatureRetentionDays:30,visitorPhoneRetentionDays:30}))await prisma.appSetting.upsert({where:{key},update:{},create:{key,value}});console.log("Visitor configuration is ready.")}
main().finally(()=>prisma.$disconnect());