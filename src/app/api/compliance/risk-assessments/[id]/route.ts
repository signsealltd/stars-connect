import { NextRequest,NextResponse } from "next/server";import { withCapability } from "@/lib/api";import { CAPABILITIES } from "@/lib/permissions";import { prisma } from "@/lib/prisma";import { requireOrganisation } from "@/lib/compliance-service";
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){return withCapability(req,CAPABILITIES.RISK_ASSESSMENT_VIEW,async user=>{const organisationId=requireOrganisation(user),{id}=await params;const record=await prisma.complianceRecord.findFirst({where:{id,organisationId,recordType:"RISK_ASSESSMENT"},include:{versions:{orderBy:{version:"desc"},include:{hazards:{orderBy:{sortOrder:"asc"}},approvals:true,attachments:{select:{id:true,originalName:true,mimeType:true,fileSize:true,createdAt:true}}}}}});return record?NextResponse.json(record):NextResponse.json({error:"Risk assessment not found."},{status:404});});}
