import "@/app/rams-pilot.css";import {Header} from "@/components/header";import {RamsManager} from "@/components/rams-manager";import {requirePageCapability,CAPABILITIES as C} from "@/lib/permissions";export default async function Page(){await requirePageCapability(C.RAMS_VIEW);return <main className="shell"><Header manager/><div className="content"><a href="/dashboard/premises">← Safety &amp; Compliance</a><h1 className="page-title">RAMS Studio</h1><RamsManager/></div></main>}
