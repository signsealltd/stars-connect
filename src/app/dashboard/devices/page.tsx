import { Header } from "@/components/header";import { DeviceManager } from "@/components/device-manager";import{requireRole}from"@/lib/security";
export default async function DevicesPage(){await requireRole("ADMINISTRATOR");return <main className="shell"><Header manager/><div className="content"><div className="page-head"><div><h1 className="page-title">Devices</h1><p className="muted">Provision, monitor, rotate and revoke authorised tablets. Device credentials are separate from manager accounts.</p></div></div><DeviceManager/><section className="card" style={{padding:22,marginTop:18}}><h2>Tablet setup</h2><ol><li>Create a uniquely named device above.</li><li>Open <code>/setup</code> on the physical tablet.</li><li>Enter the one-time code before it expires.</li><li>Confirm the tablet appears healthy after its first successful sync.</li></ol></section></div></main>}