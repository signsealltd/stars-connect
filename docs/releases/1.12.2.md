# V1.12.2 — Operational calendar access

Removed the obsolete manager/director/administrator pilot restriction from the
calendar page. Its `calendar.view` capability check now matches the navigation
and calendar API, allowing configured Team Leader access. The feature switch,
calendar management capability and operation assignment permissions remain enforced.

No database migration or permission changes are required for this patch.

Seven focused tests passed, including rendering the actual calendar page for a
Team Leader with view-only access, denied Team Leader and manager access, and the
disabled feature switch.

Version: V1.12.1 → V1.12.2.
Production build passed, with existing lint warnings only.
