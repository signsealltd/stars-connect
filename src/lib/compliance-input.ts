import { z } from "zod";
const optionalText=(max:number)=>z.string().trim().max(max).nullable().optional();const optionalDate=z.string().date().nullable().optional().or(z.literal(""));
export const hazardInput=z.object({id:z.string().uuid().optional(),sortOrder:z.number().int().min(0).max(1000).default(0),hazard:z.string().trim().min(2).max(5000),whoMayBeHarmed:z.string().trim().min(2).max(5000),howTheyMayBeHarmed:z.string().trim().min(2).max(5000),existingControls:z.string().trim().min(2).max(10000),initialLikelihood:z.number().int().min(1).max(5),initialSeverity:z.number().int().min(1).max(5),furtherControls:optionalText(10000),actionOwnerId:z.string().uuid().nullable().optional(),targetDate:optionalDate,residualLikelihood:z.number().int().min(1).max(5),residualSeverity:z.number().int().min(1).max(5)});
export const riskAssessmentCreateInput=z.object({reference:z.string().trim().min(2).max(80),title:z.string().trim().min(2).max(191),categoryId:z.string().uuid().nullable().optional(),description:optionalText(20000),scope:optionalText(20000),premisesLocation:optionalText(191),relatedActivity:optionalText(191),relatedAssetId:z.string().uuid().nullable().optional(),assessorId:z.string().uuid().nullable().optional(),responsibleManagerId:z.string().uuid().nullable().optional(),assessmentDate:optionalDate,reviewDate:optionalDate,internalNotes:optionalText(20000),revisionNotes:optionalText(10000),tags:z.array(z.string().trim().min(1).max(80)).max(30).optional(),hazards:z.array(hazardInput).max(100).default([])}).strict();
export const riskAssessmentUpdateInput=riskAssessmentCreateInput.omit({reference:true}).partial().strict();
export const workflowCommentInput=z.object({comments:z.string().trim().max(10000).optional().default("")}).strict();
export const pageInput=z.coerce.number().int().min(1).max(100000).default(1);export const pageSizeInput=z.coerce.number().int().min(1).max(100).default(25);
