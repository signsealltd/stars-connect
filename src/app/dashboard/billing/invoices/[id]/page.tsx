import{Header}from"@/components/header";import{prisma}from"@/lib/prisma";import{CAPABILITIES,requireCapability}from"@/lib/permissions";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){await requireCapability(CAPABILITIES.BILLING_REVIEW);const{id}=await params,i=await prisma.invoice.findUniqueOrThrow({where:{id}});return <main className="shell"><Header manager/><div className="content"><h1>Invoice {i.invoiceNumber}</h1><section className="card"><p><b>Payer:</b> {i.payerName}</p><p><b>Status:</b> {i.status}</p><p><b>Net:</b> £{Number(i.netTotal).toFixed(2)} · <b>VAT:</b> £{Number(i.vatTotal).toFixed(2)} · <b>Total:</b> £{Number(i.grossTotal).toFixed(2)}</p>{i.documentId&&<a className="btn primary" href={`/api/documents/${i.documentId}/download`}>Download invoice PDF</a>}</section></div></main>}