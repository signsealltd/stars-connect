import{Header}from"@/components/header";import{SmtpConfigurationForm}from"@/components/smtp-configuration-form";import{EmailSettingsPanel}from"@/components/email-settings-panel";import{CAPABILITIES,requirePageCapability}from"@/lib/permissions";export const dynamic="force-dynamic";export default async function Page(){await requirePageCapability(CAPABILITIES.REPORT_SETTINGS_MANAGE);return <main className="shell"><Header manager/><div className="content"><h1 className="page-title">Email</h1><p className="muted">Safe SMTP diagnostics and test delivery. Credentials and job secrets are never displayed.</p><div className="grid" style={{marginBottom:20}}><SmtpConfigurationForm/></div><EmailSettingsPanel/></div></main>}