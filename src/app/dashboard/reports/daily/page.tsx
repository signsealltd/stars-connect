import{Header}from"@/components/header";import{FinanceConsole}from"@/components/finance-console";import{requireCapability,CAPABILITIES}from"@/lib/permissions";export const dynamic="force-dynamic";export default async function Page(){await requireCapability(CAPABILITIES.DAILY_REPORT_VIEW);return <main className="shell"><Header manager/><div className="content"><h1 className="page-title">Daily attendance reports</h1><p className="muted">Immutable, versioned reports for the previous complete Europe/London calendar day.</p><FinanceConsole mode="daily"/></div></main>}